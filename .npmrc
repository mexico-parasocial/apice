# Use hoisted node_modules for Expo / React Native compatibility.
# This makes pnpm layout behave similarly to npm/yarn classic.
node-linker=hoisted

# Be lenient about peer-dependency mismatches during the migration.
strict-peer-dependencies=false

# Ensure shell-emulator for cross-platform lifecycle scripts (optional but safe).
# shell-emulator=true
