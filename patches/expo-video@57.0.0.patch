diff --git a/ios/VideoPlayer.swift b/ios/VideoPlayer.swift
index 1111111..2222222 100644
--- a/ios/VideoPlayer.swift
+++ b/ios/VideoPlayer.swift
@@ -454,7 +454,7 @@
 
   func safeEmit(event: String, payload: Record? = nil) {
     if self.appContext != nil {
-      self.emit(event: event, payload: payload?.toDictionary(appContext: appContext))
+      self.emit(event: event, arguments: payload?.toDictionary(appContext: appContext))
     }
   }
 
diff --git a/ios/VideoPlayerAudioTracks.swift b/ios/VideoPlayerAudioTracks.swift
index 1111111..2222222 100644
--- a/ios/VideoPlayerAudioTracks.swift
+++ b/ios/VideoPlayerAudioTracks.swift
@@ -1,5 +1,6 @@
 import Foundation
 import AVFoundation
+import OSLog
 
 class VideoPlayerAudioTracks {
   weak var owner: VideoPlayer?
@@ -25,7 +26,7 @@
       availableAudioTracks = try await Self.findAvailableAudioTracks(for: playerItem)
     } catch {
       let uri = (playerItem as? VideoPlayerItem)?.videoSource.uri?.absoluteString ?? "the current source"
-      owner?.appContext?.jsLogger.warn("Failed to load available audio tracks for \(uri). Error: \(error)")
+      os_log(.error, log: OSLog(subsystem: "dev.expo.modules", category: "expo-video"), "Failed to load available audio tracks for %@. Error: %@", uri, "\(error)")
       availableAudioTracks = []
     }
 
diff --git a/ios/VideoPlayerSubtitles.swift b/ios/VideoPlayerSubtitles.swift
index 1111111..2222222 100644
--- a/ios/VideoPlayerSubtitles.swift
+++ b/ios/VideoPlayerSubtitles.swift
@@ -1,5 +1,6 @@
 import Foundation
 import AVFoundation
+import OSLog
 
 class VideoPlayerSubtitles {
   weak var owner: VideoPlayer?
@@ -25,7 +26,7 @@
       availableSubtitleTracks = try await Self.findAvailableSubtitleTracks(for: playerItem)
     } catch {
       let uri = (playerItem as? VideoPlayerItem)?.videoSource.uri?.absoluteString ?? "the current source"
-      owner?.appContext?.jsLogger.warn("Failed to load available subtitle tracks for \(uri). Error: \(error)")
+      os_log(.error, log: OSLog(subsystem: "dev.expo.modules", category: "expo-video"), "Failed to load available subtitle tracks for %@. Error: %@", uri, "\(error)")
       availableSubtitleTracks = []
     }
 
