packages:
  - 'packages/*'
  - 'server'
  - 'admin'

overrides:
  '@types/react': '^18.3.31'

# bufferutil / utf-8-validate are deliberately NOT built: they're optional
# WebSocket accelerators with pure-JS fallbacks, and their node-gyp build
# needs a C++ toolchain the alpine deploy images don't carry.
onlyBuiltDependencies:
  - '@prisma/client'
  - '@prisma/engines'
  - '@sentry/cli'
  - 'core-js'
  - 'prisma'
  - 'unrs-resolver'

allowBuilds:
  '@prisma/client': true
  '@prisma/engines': true
  '@sentry/cli': true
  bufferutil: false
  core-js: true
  prisma: true
  unrs-resolver: true
  utf-8-validate: false

# Declining these on purpose (see note above) — without this list pnpm 11
# fails non-interactive installs with ERR_PNPM_IGNORED_BUILDS.
ignoredBuiltDependencies:
  - bufferutil
  - utf-8-validate

patchedDependencies:
  expo-modules-autolinking@2.0.8: patches/expo-modules-autolinking@2.0.8.patch
  expo-sharing@57.0.6: patches/expo-sharing@57.0.6.patch
